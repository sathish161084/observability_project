apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd-image-updater-self
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://argoproj.github.io/argo-helm
    chart: argocd-image-updater
    targetRevision: 0.11.0
    helm:
      values: |
        config:
          registries:
            - name: ECR
              prefix: ACCOUNT_ID.dkr.ecr.eu-west-1.amazonaws.com
              api_url: https://ACCOUNT_ID.dkr.ecr.eu-west-1.amazonaws.com
              credentials: ext:/scripts/login.sh
        authScripts:
          enabled: true
          scripts:
            login.sh: |
              aws ecr get-authorization-token --region eu-west-1 --output text --query 'authorizationData[].authorizationToken' | base64 -d
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
