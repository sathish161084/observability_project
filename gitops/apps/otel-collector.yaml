apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: otel-collector
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/REPLACE_ME/three-tier-eks-platform-prod.git
    targetRevision: HEAD
    path: helm/otel-collector

    helm:
      valueFiles:
        - ../../gitops/values/common-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: observability
  syncPolicy:
    automated: { prune: true, selfHeal: true }
